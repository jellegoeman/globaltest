importFrom(graphics, hist)

importFrom(stats, coefficients, fitted.values, residuals, weights, alias, 
	model.matrix, p.adjust, p.adjust.methods)

importFrom(annotate, lookUp)

importFrom(AnnotationDbi, Term, Secondary, as.list, Ontology, mappedkeys, keys)

importFrom(survival, Surv, coxph, coxph.fit, basehaz, coxph.control, untangle.specials)

import(Biobase, methods)

importFrom("grDevices", "dev.off", "gray", "rainbow", "rgb")

importFrom("graphics", "abline", "arrows", "axis", "barplot", "layout",
           "lines", "locator", "par", "plot", "strwidth", "text",
           "title")

importFrom("stats", "as.dendrogram", "as.dist", "as.formula", "coef",
           "cor", "dist", "dnorm", "formula", "glm", "glm.control",
           "hclust", "heatmap", "integrate", "is.leaf", "lm",
           "model.frame", "model.offset", "pf", "pnorm", "quantile",
           "reorder", "terms", "terms.formula", "var")

importFrom("utils", "flush.console")

exportClasses(mlogit, gt.object)

exportMethods("[", "[[", show, result, p.value, length, names, "names<-", 
	sort, size, hist, z.score, summary, residuals, 
	fitted.values, coefficients, alias, "alias<-", model.matrix, 
	weights, p.adjust, subsets, extract, sterms)

export(gt, mlogit,
	gtGO, combine, leafNodes, draw, gtKEGG, gtBroad, gtConcept,
	comparative, focusLevel, inheritance, covariates, features, subjects, 
	gt.options, findFocus, gtPS, gtKS, gtLI, bbase, btensor, reparamZ, reweighZ)
