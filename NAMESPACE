importFrom(graphics, plot, hist)

importFrom(survival, Surv, coxph, coxph.fit, basehaz, coxph.control)

import(Biobase)

exportClasses(gt.result, gt.barplot)

exportMethods("[", show, result, p.value, length, names, "names<-", 
	sort, hist, z.score, scale, plot, combine, fit)

export(globaltest, permutations, sampling, geneplot, sampleplot, regressionplot, checkerboard, multtest)
